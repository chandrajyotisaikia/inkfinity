# Cloudinary Configuration - Secure Environment Variables
# IMPORTANT: This is a TEMPLATE file. Copy to .env.local and fill in your credentials.
# .env.local should NEVER be committed to Git (add to .gitignore)

# Cloudinary Cloud Name (Public - safe to expose)
VITE_CLOUDINARY_CLOUD_NAME=ugwrb2sw

# Cloudinary API Key (Public - safe to expose)
VITE_CLOUDINARY_API_KEY=478593486576879

# Cloudinary API Secret (PRIVATE - NEVER expose in client code)
# This should only be used in backend/server environments
VITE_CLOUDINARY_API_SECRET=your_actual_secret_key_here

# Usage in React components:
# const cloudName = import.meta.env.VITE_CLOUDINARY_CLOUD_NAME
# const apiKey = import.meta.env.VITE_CLOUDINARY_API_KEY
# 
# NOTE: API_SECRET should NEVER be used in browser code.
# For PDF uploads/management, use a backend proxy or Cloudinary's unsigned uploads.
